{
  "name": "leaveapp-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd leaveapp && npm install && npm run build",
    "dev": "cd leaveapp && npm run dev",
    "start": "cd leaveapp && npm run start",
    "postinstall": "cd leaveapp && npm install"
  },
  "dependencies": {
    "next": "14.1.0",
    "react": "^18",
    "react-dom": "^18"
  }
}
